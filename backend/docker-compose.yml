services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: aurore
      POSTGRES_PASSWORD: aurore
      POSTGRES_DB: aurore
    ports:
      - "5432:5432"
    volumes:
      - aurore_pg_data:/var/lib/postgresql/data

volumes:
  aurore_pg_data:
